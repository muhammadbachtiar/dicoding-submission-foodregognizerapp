name: submission
description: "Food Recognizer App - Dicoding Submission"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  image_picker: ^1.1.2
  image_cropper: ^9.0.0
  tflite_flutter: ^0.12.0
  image: ^4.5.4
  http: ^1.4.0
  archive: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
